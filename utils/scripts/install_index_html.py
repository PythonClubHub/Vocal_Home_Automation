# Check if apache server is installed. Folder /var/www/html/ should exist

# If apache server is not installed an error mesage should be printed informing the user to install the apache server (and how)

# If apache server is installed, copy the inndex.html from this repo to /var/www/html
